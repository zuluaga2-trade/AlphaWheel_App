# AlphaWheel Pro — Ejemplo de Secrets (solo para uso local)
# Copia este archivo como secrets.toml y sustituye con tus emails autorizados.
# NO subas secrets.toml a GitHub (está en .gitignore).

# Lista de emails que pueden registrarse e iniciar sesión (separados por coma).
# Si lo dejas vacío o no defines esta variable, se permiten todos los emails (desarrollo local).
ALPHAWHEEL_ALLOWED_EMAILS = "email1@ejemplo.com,email2@ejemplo.com"

# Opcional: caché compartida entre usuarios (mismo ticker = mismo resultado para todos, menos llamadas a APIs).
# Puedes usar el nombre ALPHAWHEEL_* o el equivalente; la app acepta cualquiera de los dos.
# Tradier (caché compartida por ticker):
#   ALPHAWHEEL_TRADIER_QUOTE_TOKEN = "tu_token_tradier_solo_lectura"
#   TRADIER_QUOTE_TOKEN = "tu_token_tradier_solo_lectura"
# Alpha Vantage (earnings + overview compartidos):
#   ALPHAWHEEL_AV_KEY = "tu_api_key_alphavantage"
#   AV_KEY = "tu_api_key_alphavantage"
